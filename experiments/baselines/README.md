# Baselines

Use this folder for cheap and explainable baseline studies.

Baseline families:

- manual rule baseline
- keyword/risk-pattern baseline
- OCR + keyword baseline
- classical ML text baseline
- transformer text baseline
- simple image embedding/classifier baseline
- hybrid baseline with human review

Rule: do not add expensive models before documenting what the cheap baseline can and cannot do.
